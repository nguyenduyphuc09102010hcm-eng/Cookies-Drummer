<!DOCTYPE html>

<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cookie Drummer - Freshly Baked Delights</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

```
    body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        background: #1a1a2e;
        color: #fff;
        overflow-x: hidden;
    }

    .hero {
        position: relative;
        height: 100vh;
        background: linear-gradient(135deg, #ff6b35 0%, #f7931e 50%, #ffd93d 100%);
        display: flex;
        align-items: center;
        justify-content: center;
        text-align: center;
        overflow: hidden;
    }

    .hero::before {
        content: '';
        position: absolute;
        width: 200%;
        height: 200%;
        background: 
            radial-gradient(circle at 20% 50%, rgba(255, 255, 255, 0.1) 0%, transparent 50%),
            radial-gradient(circle at 80% 80%, rgba(255, 107, 53, 0.2) 0%, transparent 50%);
        animation: float 20s ease-in-out infinite;
    }

    @keyframes float {
        0%, 100% { transform: translate(0, 0) rotate(0deg); }
        50% { transform: translate(-50px, -50px) rotate(180deg); }
    }

    .hero-content {
        position: relative;
        z-index: 2;
        padding: 40px;
    }

    .logo {
        font-size: 5em;
        margin-bottom: 20px;
        animation: bounce 2s ease-in-out infinite;
    }

    @keyframes bounce {
        0%, 100% { transform: translateY(0); }
        50% { transform: translateY(-20px); }
    }

    .hero h1 {
        font-size: 4.5em;
        font-weight: 900;
        text-shadow: 4px 4px 8px rgba(0, 0, 0, 0.3);
        margin-bottom: 20px;
        letter-spacing: 2px;
    }

    .hero p {
        font-size: 1.8em;
        margin-bottom: 40px;
        text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.2);
    }

    .cta-buttons {
        display: flex;
        gap: 20px;
        justify-content: center;
        flex-wrap: wrap;
    }

    .btn {
        padding: 18px 45px;
        font-size: 1.2em;
        font-weight: 700;
        border: none;
        border-radius: 50px;
        cursor: pointer;
        transition: all 0.3s;
        text-decoration: none;
        display: inline-block;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
    }

    .btn-primary {
        background: #fff;
        color: #ff6b35;
    }

    .btn-primary:hover {
        transform: translateY(-5px);
        box-shadow: 0 15px 40px rgba(0, 0, 0, 0.4);
    }

    .btn-secondary {
        background: rgba(255, 255, 255, 0.2);
        color: #fff;
        border: 3px solid #fff;
    }

    .btn-secondary:hover {
        background: rgba(255, 255, 255, 0.3);
        transform: translateY(-5px);
    }

    .section {
        padding: 100px 40px;
        position: relative;
    }

    .menu-section {
        background: linear-gradient(180deg, #1a1a2e 0%, #16213e 100%);
    }

    .section-title {
        font-size: 3.5em;
        text-align: center;
        margin-bottom: 60px;
        color: #ffd93d;
        text-shadow: 3px 3px 6px rgba(0, 0, 0, 0.5);
    }

    .menu-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
        gap: 50px;
        max-width: 1200px;
        margin: 0 auto;
    }

    .menu-item {
        background: linear-gradient(135deg, #ff6b35 0%, #f7931e 100%);
        border-radius: 30px;
        padding: 40px;
        text-align: center;
        box-shadow: 0 20px 60px rgba(255, 107, 53, 0.4);
        transition: all 0.4s;
        position: relative;
        overflow: hidden;
    }

    .menu-item::before {
        content: '';
        position: absolute;
        top: -50%;
        left: -50%;
        width: 200%;
        height: 200%;
        background: radial-gradient(circle, rgba(255, 255, 255, 0.2) 0%, transparent 70%);
        animation: rotate 10s linear infinite;
    }

    @keyframes rotate {
        from { transform: rotate(0deg); }
        to { transform: rotate(360deg); }
    }

    .menu-item:hover {
        transform: translateY(-15px) scale(1.05);
        box-shadow: 0 30px 80px rgba(255, 107, 53, 0.6);
    }

    .menu-icon {
        font-size: 6em;
        margin-bottom: 20px;
        position: relative;
        z-index: 1;
    }

    .menu-item h3 {
        font-size: 2.2em;
        margin-bottom: 15px;
        position: relative;
        z-index: 1;
    }

    .menu-item p {
        font-size: 1.2em;
        margin-bottom: 25px;
        opacity: 0.95;
        position: relative;
        z-index: 1;
    }

    .price {
        font-size: 2em;
        font-weight: 900;
        color: #fff;
        text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
        position: relative;
        z-index: 1;
    }

    .contact-section {
        background: linear-gradient(135deg, #0f3460 0%, #16213e 100%);
    }

    .contact-container {
        max-width: 900px;
        margin: 0 auto;
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(10px);
        border-radius: 30px;
        padding: 60px;
        box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
    }

    .contact-info {
        text-align: center;
        margin-bottom: 50px;
    }

    .phone-number {
        font-size: 2.5em;
        font-weight: 900;
        color: #ffd93d;
        margin: 20px 0;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 15px;
    }

    .phone-icon {
        font-size: 1.2em;
        animation: ring 2s ease-in-out infinite;
    }

    @keyframes ring {
        0%, 100% { transform: rotate(0deg); }
        10%, 30% { transform: rotate(-15deg); }
        20%, 40% { transform: rotate(15deg); }
    }

    .contact-form {
        display: grid;
        gap: 25px;
    }

    .form-group {
        display: flex;
        flex-direction: column;
    }

    label {
        font-size: 1.2em;
        margin-bottom: 10px;
        color: #ffd93d;
        font-weight: 600;
    }

    input, textarea, select {
        padding: 18px;
        border: 3px solid rgba(255, 217, 61, 0.3);
        border-radius: 15px;
        font-size: 1.1em;
        background: rgba(255, 255, 255, 0.1);
        color: #fff;
        transition: all 0.3s;
    }

    input:focus, textarea:focus, select:focus {
        outline: none;
        border-color: #ffd93d;
        background: rgba(255, 255, 255, 0.15);
        box-shadow: 0 0 20px rgba(255, 217, 61, 0.3);
    }

    textarea {
        resize: vertical;
        min-height: 150px;
        font-family: inherit;
    }

    select option {
        background: #16213e;
        color: #fff;
    }

    .submit-btn {
        background: linear-gradient(135deg, #ff6b35 0%, #f7931e 100%);
        color: #fff;
        padding: 20px;
        font-size: 1.4em;
        font-weight: 700;
        border: none;
        border-radius: 50px;
        cursor: pointer;
        transition: all 0.3s;
        box-shadow: 0 10px 30px rgba(255, 107, 53, 0.5);
        margin-top: 20px;
    }

    .submit-btn:hover {
        transform: translateY(-5px);
        box-shadow: 0 15px 40px rgba(255, 107, 53, 0.7);
    }

    .scroll-arrow {
        position: absolute;
        bottom: 40px;
        left: 50%;
        transform: translateX(-50%);
        font-size: 3em;
        animation: bounce-arrow 2s ease-in-out infinite;
        cursor: pointer;
        z-index: 10;
    }

    @keyframes bounce-arrow {
        0%, 100% { transform: translateX(-50%) translateY(0); }
        50% { transform: translateX(-50%) translateY(20px); }
    }

    @media (max-width: 768px) {
        .hero h1 { font-size: 2.5em; }
        .hero p { font-size: 1.3em; }
        .section-title { font-size: 2.5em; }
        .menu-grid { grid-template-columns: 1fr; }
        .contact-container { padding: 40px 30px; }
        .phone-number { font-size: 1.8em; }
    }
</style>
```

</head>
<body>
    <section class="hero" id="home">
        <div class="hero-content">
            <div class="logo">🥁🍪</div>
            <h1>COOKIE DRUMMER</h1>
            <p>Beating Up Fresh Flavors Daily!</p>
            <div class="cta-buttons">
                <a href="#menu" class="btn btn-primary">View Menu</a>
                <a href="#contact" class="btn btn-secondary">Order Now</a>
            </div>
        </div>
        <div class="scroll-arrow" onclick="document.getElementById('menu').scrollIntoView({behavior: 'smooth'})">↓</div>
    </section>

```
<section class="section menu-section" id="menu">
    <h2 class="section-title">🎵 OUR SIGNATURE BEATS 🎵</h2>
    <div class="menu-grid">
        <div class="menu-item">
            <div class="menu-icon">🍪</div>
            <h3>Choco Chip Cookie</h3>
            <p>Classic rhythm with rich chocolate chips that'll make your taste buds dance!</p>
            <div class="price">15.000 VNĐ</div>
        </div>

        <div class="menu-item">
            <div class="menu-icon">🥧</div>
            <h3>Egg Tart</h3>
            <p>Smooth and creamy Portuguese-style egg custard in a crispy, flaky pastry shell!</p>
            <div class="price">25.000 VNĐ</div>
        </div>
    </div>
</section>

<section class="section contact-section" id="contact">
    <h2 class="section-title">🎸 DROP US A LINE 🎸</h2>
    <div class="contact-container">
        <div class="contact-info">
            <h3 style="font-size: 2em; margin-bottom: 20px;">Get In Touch!</h3>
            <div class="phone-number">
                <span style="font-size: 1.2em;">📘</span>
                <a href="https://www.facebook.com/share/14MEbwm7ZWa/?mibextid=wwXIfr" target="_blank" style="color: #ffd93d; text-decoration: none;">Message us on Facebook</a>
            </div>
            <p style="font-size: 1.2em; opacity: 0.9; margin-top: 20px;">Or fill out the form below and we'll get back to you!</p>
        </div>

        <form class="contact-form" id="orderForm">
            <div class="form-group">
                <label for="name">Your Name *</label>
                <input type="text" id="name" name="name" required placeholder="Enter your name">
            </div>

            <div class="form-group">
                <label for="class">Lớp *</label>
                <input type="text" id="class" name="class" required placeholder="Ví dụ: 10A1, 11B2...">
            </div>

            <div class="form-group">
                <label for="school">Trường *</label>
                <input type="text" id="school" name="school" required placeholder="Tên trường của bạn">
            </div>

            <div class="form-group">
                <label for="facebook">Facebook Link</label>
                <input type="url" id="facebook" name="facebook" placeholder="https://facebook.com/yourprofile">
            </div>

            <div class="form-group">
                <label for="product">What Would You Like? *</label>
                <select id="product" name="product" required>
                    <option value="">Select your treat...</option>
                    <option value="choco-chip">Choco Chip Cookie</option>
                    <option value="egg-tart">Egg Tart</option>
                    <option value="both">Both!</option>
                </select>
            </div>

            <div class="form-group">
                <label for="quantity">Quantity</label>
                <input type="number" id="quantity" name="quantity" min="1" placeholder="How many?">
            </div>

            <div class="form-group">
                <label for="message">Message / Special Requests</label>
                <textarea id="message" name="message" placeholder="Tell us about your order, delivery preferences, or any special requests..."></textarea>
            </div>

            <button type="submit" class="submit-btn">🎵 Send Order Request 🎵</button>
        </form>
    </div>
</section>

<script>
    document.getElementById('orderForm').addEventListener('submit', function(e) {
        e.preventDefault();
        
        const name = document.getElementById('name').value;
        const phone = document.getElementById('phone').value;
        const product = document.getElementById('product').value;
        
        alert('🎉 Thank you, ' + name + '! We received your order request. We\'ll contact you at ' + phone + ' soon to confirm your delicious treats!');
        
        this.reset();
    });

    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
        anchor.addEventListener('click', function (e) {
            e.preventDefault();
            const target = document.querySelector(this.getAttribute('href'));
            if (target) {
                target.scrollIntoView({ behavior: 'smooth' });
            }
        });
    });
</script>
```

</body>
</html>